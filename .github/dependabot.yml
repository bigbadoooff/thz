# Keeps the SHA-pinned GitHub Actions in .github/workflows up to date: each
# update is proposed as a PR that bumps the pin and its version comment.
version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
